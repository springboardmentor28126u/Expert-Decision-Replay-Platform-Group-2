services:
  backend:
    build: ./edrp-backend
    ports:
      - "8000:8000"
    env_file:
      - ./edrp-backend/.env
    restart: unless-stopped

  frontend:
    build: ./edrp-frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
    restart: unless-stopped