# Example environment variables for backend (do NOT commit real secrets)
APP_NAME=Expert Decision Replay Platform
DATABASE_HOST=
DATABASE_PORT=5432
DATABASE_NAME=
DATABASE_USER=
DATABASE_PASSWORD=
# Option 1: OpenRouter key (preferred for this project)
OPENROUTER_API_KEY=
# Option 2: OpenAI-compatible key (fallback)
OPENAI_API_KEY=
# SMTP / email settings
SMTP_SERVER=
SMTP_PORT=
SMTP_EMAIL=
SMTP_PASSWORD=
